<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.nottodolist.daily.DailyMapper">

    <update id="updDaily">
        UPDATE t_use_list
        SET
        use_cost = #{cost}
        WHERE
        use_list_id = #{useListId}
    </update>

    <select id="selDailyAll">
        select C.name,format(A.use_cost,0) cost,B.cost_category costCategoryId,A.use_list_id useListId
        from t_use_list as A
        inner join t_monthly_goal as B
        on A.goal_id=B.goal_id
        inner join t_not_todo as C
        on C.not_todo_id=B.not_todo_id
        where date_format(date,'%Y-%m-%d')=#{date}
    </select>

    <update id="delDaily">
        update t_use_list
        set use_cost=0
        where use_list_id=#{useListId}
    </update>

</mapper>