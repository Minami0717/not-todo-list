<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nottodolisttest.Calender.CalenderMapper">

    <select id="selCalender">
    select C.name,A.date
    from t_use_list as A
    inner join t_monthly_goal as B
    on A.goal_id=B.goal_id
    inner join t_not_todo_list as C
    on C.not_todo_id=B.not_todo_id
    where use_yn = 1
    </select>




</mapper>